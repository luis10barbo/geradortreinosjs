// // @ts-check

// import { sqliteTable } from "drizzle-orm/sqlite-core";
// import { integer, real, text } from "drizzle-orm/sqlite-core";

// export const tabelaVersoes = sqliteTable("versao", {
//   id: integer("id").primaryKey(),
//   versao: real("versao").notNull(),
//   nome: text("nome"),
//   download: text("download").notNull(),
// });
// /** @typedef {import("drizzle-orm").InferSelectModel<typeof tabelaVersoes>} Versao*/
